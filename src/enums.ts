export enum DropCategory{
    SampleTraySample,
    EDITOR,
    PLACEHOLDER,
    IFRAME,
}

export enum DragSource {
    SAMPLE_TRAY
}
  
export enum InteractionType {
    ACTIVE = "ACTIVE",
    STATIC = "STATIC",
  }
  export enum AppMode {
    DISPLAY = "DISPLAY",
    EDIT = "EDIT",
    CYCLE = "CYCLE",
  }