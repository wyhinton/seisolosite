//describes the column names in a spreadsheet containing card data
export default interface RawCardRow {
  src: string;
  title: string;
  added: string;
  sourceid: string;
  author: string;
  interaction: string;
}
